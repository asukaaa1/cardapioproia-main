-- Legacy admin seeding removed from source control to avoid hardcoded personal data.
-- Admin roles should be assigned through the authenticated admin panel or a one-off
-- SQL statement executed directly in the Supabase dashboard.
SELECT 1;
